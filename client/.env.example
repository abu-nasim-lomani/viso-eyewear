# Frontend env (plan §15). Copy to .env for local dev.
# In dev, Vite proxies /api → http://localhost:5000 (see vite.config.js),
# so this can stay as the default until the backend is deployed.
VITE_API_URL=/api
